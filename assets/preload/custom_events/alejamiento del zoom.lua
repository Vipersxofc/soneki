local startedAnim = false;
local didEvent = false;

function onEvent(name,value1,value2)
	if name == 'alejamiento del zoom ' then
		"defaultZoom": 0.5,
		"isPixelStage": false,
		--debugPrint('Event triggered: ', name, duration, targetAlpha);
	end
end